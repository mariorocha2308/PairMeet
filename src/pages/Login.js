import { Text, View } from "dripsy";

const Login = () => {
  return ( 
    <View sx={{flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: 'white'}}>
      <Text>Login</Text>
    </View>
  );
}

export default Login;